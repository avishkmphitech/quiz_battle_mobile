# Copy to assets/env/.env — only API_BASE_URL is read here (see EnvConfig).
# Contract reference: backend/src/modules/auth (and related routes).

API_BASE_URL=https://8t48djhs-5000.inc1.devtunnels.ms
